import { RequestOptions, OAuthOptions, ExtendedOAuthOptions } from "../types";
export default function signature(options: RequestOptions & {
    oAuthOptions: OAuthOptions & ExtendedOAuthOptions;
}): string;
